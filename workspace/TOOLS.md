# Tools

Available workspace tools:

- `get_morning_recap_context`

Usage:

- For the scheduled morning recap, use `get_morning_recap_context` first.
- For recap follow-up questions, use the same tool again if fresh context is needed.
